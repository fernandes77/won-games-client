import { prefixAll } from '@/utils/tailwind/prefix-all'

export const minHuge = (classNames: string) => prefixAll('min-huge', classNames)

export const maxHuge = (classNames: string) => prefixAll('max-huge', classNames)

export const minLg = (classNames: string) => prefixAll('min-lg', classNames)

export const maxLg = (classNames: string) => prefixAll('max-lg', classNames)

export const minMd = (classNames: string) => prefixAll('min-md', classNames)

export const maxMd = (classNames: string) => prefixAll('max-md', classNames)

export const minSm = (classNames: string) => prefixAll('min-sm', classNames)

export const maxSm = (classNames: string) => prefixAll('max-sm', classNames)
